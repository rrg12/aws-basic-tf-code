name: Test
on: push
jobs:
  build:
    runs-on:
      - ubuntu-latest
    steps:
    - name: Testing
      env:
        SECRET: ${{secrets.TEST}}
      run: |
        echo "Secret is ${SECRET}"
          
          
